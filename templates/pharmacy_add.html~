<!DOCTYPE html>
<html>
    <head>
        <title>Pharmacy Sign Up</title>
        <link href="/static/bootstrap.min.css" rel="stylesheet">
        <link href="/static/jumbotron-narrow.css" rel="stylesheet">
        <link href="/static/signup.css" rel="stylesheet">
    </head>
    <body>
        <div class="container">
            <div class="header">
                <nav>
                    <ul class="nav nav-pills pull-right">
                        <li role="presentation"><a href="{{ url_for('pharmacy_login') }}">Sign In</a></li>
                        <li role="presentation" class="active"><a href="#">Sign Up</a></li>
                    </ul>
                </nav>
                <h3 class="text-muted">Sintagografisi Web App</h3>
            </div>
            
            <div class="jumbotron">
                <h2>Sign Up</h2>
                    {% if error %}
                        <div style="color: red"> {{error}} </div>
                    {% endif %}
                    <form action="" method="post" class="form-signin">
                        <label for="username" class="sr-only">Username</label>
                        <input type="text" name="username" id="username" class="form-control" placeholder="Όνομα Χρήστη" required autofocus>
                        <label for="password" class="sr-only">Password</label>
                        <input type="password" name="password" id="password" class="form-control" placeholder="Κωδικός" required>
                        <label for="password-repeat" class="sr-only">Password Repeat</label>
                        <input type="password" name="password-repeat" id="password-repeat" class="form-control" placeholder="Επαναλάβετε τον κωδικό" required>
                        <label for="name" class="sr-only">Name</label>
                        <input type="text" name="name" id="name" class="form-control" placeholder="Όνομα" required>
                        <label for="area" class="sr-only">Area</label>
                        <input type="text" name="area" id="area" class="form-control" placeholder="Περιοχή" required>
                        <label for="street" class="sr-only">Street</label>
                        <input type="text" name="street" id="street" class="form-control" placeholder="Οδός" required>
                        <label for="street_number" class="sr-only">Street Number</label>
                        <input type="number" name="street_number" id="street_number" class="form-control" min="0" placeholder="Αριθμός" required>
                        <button id="btnSignIn" class="btn btn-lg btn-primary btn-block" type="submit">Εγγραφή</button>
                    </form>
            </div>
            
        </div>
    </boby>
</html>
